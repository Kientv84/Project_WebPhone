import React from 'react'
import { Descriptions, FloatButton } from 'antd';

const Descomponet = () => {
    return (
        <div>
            <FloatButton onClick={() => console.log(Descriptions)} />;
        </div>
    )
}

export default Descomponet
